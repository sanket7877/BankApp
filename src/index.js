import React from "react";
import ReactDOM from "react-dom";
import Todo from "./todo";
import "./style.css";

function App() {
    return (
          <div>
            <h1>good morning new data applied</h1>
          </div>
        );
}

const rootElement = document.getElementById("root");
ReactDOM.render(<App />, rootElement);
